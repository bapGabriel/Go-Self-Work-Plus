{
    "name": "go-self-work-plus-main",
    "private": true,
    "scripts": {
        "install:all": "npm install && npm install --prefix back && npm install --prefix front",
        "dev": "concurrently \"npm run dev --prefix back\" \"npm run dev --prefix front\""
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}